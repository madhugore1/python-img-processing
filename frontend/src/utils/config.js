export const config = {
  BASE_URL: "http://localhost:8000",
  endpoints: {
    rotate: "/rotate",
    grayscale: "/grayscale",
    thumbnail: "/thumbnail",
    compress: "/compress",
    apply_filter: "/apply_filter",
  },
};
